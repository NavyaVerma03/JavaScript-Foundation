// Question: Take two numbers and divide them using / operator.

let a = Number(prompt("Enter first number:"));
let b = Number(prompt("Enter second number:"));

let div = a / b;

console.log("Division =", div);