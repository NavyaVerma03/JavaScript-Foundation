// Question: Take two numbers and multiply them using * operator.

let a = Number(prompt("Enter first number:"));
let b = Number(prompt("Enter second number:"));

let mul = a * b;

console.log("Multiplication =", mul);