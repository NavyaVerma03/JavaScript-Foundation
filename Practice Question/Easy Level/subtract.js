// Question: Take two numbers and subtract them using - operator.

let a = Number(prompt("Enter first number:"));
let b = Number(prompt("Enter second number:"));

let sub = a - b;

console.log("Subtraction =", sub);