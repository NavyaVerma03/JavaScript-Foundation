// Question: Take marks and print "Pass" if marks >= 33 otherwise "Fail" using ternary operator.

let marks = Number(prompt("Enter marks:"));

let result = (marks >= 33) ? "Pass" : "Fail";

console.log(result);